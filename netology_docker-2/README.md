# Домашнее задание к занятию 5. «Практическое применение Docker» — Часть 2

## Задача 0
* Старый `docker-compose` (с тире) отсутствует в системе, выдавая требуемую ошибку [INDEX].
* Новый плагин `docker compose` (без тире) установлен. Версия: **2.40.3**.
* Скриншот проверки сохранен под именем: `task0_docker_version_check.png`

## Задача 1
Создан многоэтапный (**multistage**) `Dockerfile.python` на базе образа `python:3.12-slim` с конструкцией `COPY . .` [INDEX]. Сборка протестирована.

**Лог успешной сборки:**
```text
Successfully built e2ebd5b2d286
Successfully tagged test-python-app:latest
```

## Задача 2
С помощью `yc tool` создан реестр `test` (`id: crpg8tbe7tpmhaldvj3e`) [INDEX]. Образ успешно тегирован, отправлен в реестр и просканирован на уязвимости [INDEX].

**Отчет сканирования образа (`crpcpfra0537h7vnl3h5`):**
```text
+----------------------+----------------------+---------------------+--------+--------------------------------+

|          ID          |        IMAGE         |     SCANNED AT      | STATUS |        VULNERABILITIES         |
+----------------------+----------------------+---------------------+--------+--------------------------------+

| che2snkqoq9kib48hfds | crpcpfra0537h7vnl3h5 | 2026-09-25 06:43:21 | READY  | high:44, medium:53, low:57,    |
|                      |                      |                     |        | undefined:2                    |
+----------------------+----------------------+---------------------+--------+--------------------------------+
```

## Задача 3
Работа проверена через `curl`, логи пишутся в базу данных MySQL [INDEX].
* Скриншот успешного SQL-запроса сохранен под именем: `task3_mysql_query_result.png`

## Задача 4
Написан автоматический bash-скрипт `deploy.sh` [INDEX]. Проект успешно развернут в каталоге `/opt/shvirtd-example-python` [INDEX]. 

* Скриншот повторного SQL-запроса с внешними IP-адресами сохранен под именем: `task4_production_mysql_result.png`

## Задача 5
Написан автоматический скрипт `backup.sh` для резервного копирования базы данных с помощью изолированного контейнера `schnitzler/mysqldump` в сети `backend`. Для безопасности секреты (`пароли/логины`) считываются динамически из локального файла `.env` и не коммитятся в публичный репозиторий.
* Настроен cron-task на ежеминутный запуск: `* * * * * /bin/bash /opt/shvirtd-example-python/backup.sh`
* Скриншот с ротацией бэкапов в каталоге `/opt/backup` сохранен под именем: `task5_cron_backups.png`

## Задача 6
Структура слоев проанализирована через утилиту `dive`. Целевой исполняемый файл обнаружен в финальном слое образа размером **120 MB** (команда `COPY dist/linux/amd64/terraform /bin/terraform`). Бинарный файл успешно скопирован на локальную рабочую машину.
* Скриншот анализа слоев в утилите сохранен под именем: `task6_dive_analysis.png`